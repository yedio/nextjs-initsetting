import { DefaultTheme } from 'styled-components';

const color = {
  main: '#14DBBA',
  gray: '#5D5D5D',
  dark: '#242424',
};

const layout = {
  contentWidth: '1280px',
};

export const theme: DefaultTheme = { color, layout };
